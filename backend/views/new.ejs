<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>New Post</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #1c1c1e;
      color: #e5e5e5;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    form {
      background-color: #2a2a2c;
      border: 1px solid #3a3a3c;
      border-radius: 10px;
      padding: 30px;
      width: 100%;
      max-width: 500px;
      box-shadow: 0 4px 10px rgba(255, 255, 255, 0.05);
    }

    input[type="text"],
    textarea {
      width: 100%;
      padding: 12px;
      margin-bottom: 20px;
      background-color: #1e1e20;
      border: 1px solid #3a3a3c;
      border-radius: 6px;
      color: #f0f0f0;
      font-size: 1rem;
    }

    input[type="text"]::placeholder,
    textarea::placeholder {
      color: #888;
    }

    textarea {
      resize: vertical;
      min-height: 120px;
    }

    button {
      background-color: #4da3ff;
      color: #fff;
      border: none;
      padding: 12px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 500;
      font-size: 1rem;
      width: 100%;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #1e90ff;
    }
  </style>
</head>
<body>

  <form method="post" action="http://localhost:1000/posts">
    <input type="text" name="username" placeholder="Enter username" required />
    <textarea name="content" placeholder="Content goes here..." required></textarea>
    <button type="submit">Submit</button>
  </form>

</body>
</html>
